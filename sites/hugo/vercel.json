{
  "version": 2,
  "build": {
    "env": {
      "HUGO_VERSION": "0.125.7",
      "HUGO_ENV": "production"
    }
  },
  "builds": [
    {
      "src": "build.sh",
      "use": "@vercel/static-build",
      "config": {
        "outputDirectory": "public"
      }
    }
  ]
}
