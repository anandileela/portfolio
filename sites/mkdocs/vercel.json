{
  "version": 2,
  "builds": [
    {
      "src": "mkdocs.yml",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "pip install -r requirements.txt && python -m mkdocs build",
        "outputDirectory": "site"
      }
    }
  ]
}
