{
  "version": 2,
  "builds": [
    {
      "src": "build.sh",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "./build.sh",
        "outputDirectory": "site"
      }
    }
  ]
}
