{
  "version": 2,
  "builds": [
    {
      "src": "build.sh",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "chmod +x build.sh && ./build.sh",
        "outputDirectory": "site"
      }
    }
  ]
}
