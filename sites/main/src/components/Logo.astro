---
import { Link } from 'accessible-astro-components'
import { Image } from 'astro:assets'
import circles from '../../public/img/circles.png'

/**
 * Logo Component
 *
 * @description Logo description
 */
interface Props {
  /**
   * Whether to load the image eagerly or lazily
   * @default 'eager'
   */
  loading?: 'eager' | 'lazy'
}

const { loading = 'eager' }: Props = Astro.props
---

<Link href="/" class="flex items-center gap-2">
  <Image src={circles} alt="Anandi Knuppel" width="47" height="37" loading={loading} />
  <span class="font-bold">anandiwrites.dev</span>
</Link>
